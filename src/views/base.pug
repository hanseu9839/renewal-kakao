doctype html 
html(lang="ko")
    head 
        title #{pageTitle} | #{siteName}
        body 
            h1 Welcome to DoongTalk
            main 
                block content
            include partials/footer.pug